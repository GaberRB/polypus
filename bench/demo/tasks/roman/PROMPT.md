Implement `toRoman(n)` in `solution.mjs` so that every test in `solution.test.mjs` passes.

Convert a positive integer (1..3999) to its Roman numeral string, using standard subtractive notation (4 = IV, 9 = IX, 40 = XL, 90 = XC, 400 = CD, 900 = CM).

Do NOT modify `solution.test.mjs`. Run the tests with `node --test` to verify.
