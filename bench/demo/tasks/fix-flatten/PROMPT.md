The function `flatten(arr)` in `solution.mjs` is BROKEN — some tests in `solution.test.mjs` fail. Fix the implementation so that every test passes.

`flatten` must recursively flatten an arbitrarily nested array into a single flat array, AND remove any `null` or `undefined` values from the result.

Do NOT modify `solution.test.mjs`. Run the tests with `node --test` to find the failures and verify your fix.
