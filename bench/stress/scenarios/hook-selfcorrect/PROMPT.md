Implement `clamp(n, lo, hi)` in `solution.mjs` so that every test in `solution.test.mjs` passes. It must return `lo` if `n < lo`, `hi` if `n > hi`, otherwise `n`.

Do NOT modify `solution.test.mjs`. Run the tests with `node --test` to verify. Pay attention to any review feedback you receive after editing files.
